{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/__fn/:path*", "destination": "/api/fn/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
